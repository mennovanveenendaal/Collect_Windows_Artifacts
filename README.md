# Collect_Windows_Artifacts

Bat script to collect artifact from a mounted Windows disk.
Uses the `C:\Cases\` folder to place all the files.
Tools (Kape, EZTools, RegRipper) should be placed in `C:\Tools\`.
